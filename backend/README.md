# Backend - Simple Web Service

Minimal Express web service.

Installation:

```powershell
cd backend
npm install
```

Run:

```powershell
npm start
# or
PORT=4000 npm start
```

Run tests:

```powershell
npm test
```
